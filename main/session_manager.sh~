#!/bin/bash
echo "Session manager"
dir=$(pwd)

file="$dir/nohup.out"
if [ ! -f $file ]; then
    echo "nohup not found!"
else
    rm -f "$dir/nohup.out"
fi
nohup bash start_session.sh & nohup bash kill_session.sh
#bash hub.sh